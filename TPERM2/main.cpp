#include<bits/stdc++.h>

using namespace std;

int main()
{
    const char letters[] = {'a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i', 'j'};
    int t = 0;

    scanf("%d", &t);

    for(int i = 0; i < t; i++)
    {
        int k = 0;
        scanf("%d", &k);

        int* pointer_list = (int*)malloc(sizeof(int)*k);
        pointer_list[0] = 1;
        
    }


    return 0;
}